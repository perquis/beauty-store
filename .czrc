{
  "config": {
    "commitizenEmoji": {
      "conventionalFormat": true,
      "subjectMaxLength": 200,
      "types": [
        {
          "code": ":whale:",
          "description": "Add or update the docker settings.",
          "emoji": "🐋",
          "name": "docker"
        }
      ]
    }
  },
  "path": "./node_modules/commitizen-emoji"
}
